# CLAUDE.md

@AGENTS.md

## Específico do Claude Code
- Site estático: prefira editar os arquivos direto, sem scaffolding.
- Não há build. Para verificar: abra o `.html` ou rode `python3 -m http.server`. Depois `commit` + `push` na `main` (o GitHub Pages republica sozinho).
- Mantenha este arquivo enxuto: regras não-óbvias estão no `@AGENTS.md`; não duplique aqui.
